class Solution 
{
    Node reverseList(Node head) 
    {
        // code here
        Node prev = null;
        while(head!=null)
        {
            Node temp = head.next;
            head.next = prev;
            prev = head;
            head = temp;
        }
        return prev;
    }
}
